To use the official UAEICP logo:

1. Download the official logo (PNG with transparent background works best).
2. Save it in this folder with the exact name:  logo.png
3. Refresh the app — it automatically uses logo.png instead of the built-in emblem (logo.svg).

Note: the built-in logo.svg is a neutral placeholder emblem, not the official ICP logo.
